# 62-複数のインスタンスを含むカウンターの一括選択機能実装 & 凡例表示改善

## 課題概要

1. **ワイルドカード一括選択機能**: 複数のインスタンスを持つカウンター（例：Process）に対して "*" ワイルドカードを使用した一括選択機能を実装
2. **凡例表示問題**: 大量のインスタンスがある場合にScottPlotの凡例がグラフ領域を覆ってしまう問題を解決

## フェーズ1: ワイルドカード一括選択機能

### 要件

1. 複数のインスタンスを含むカウンターに "*" を追加
2. "*" を選択すると、すべてのインスタンスの該当カウンターを取得
3. "*" は先頭に表示
4. 複数のインスタンスを含まないカウンターには "*" を表示しない

## 実装方法

### 1. CounterTreeNode クラスの拡張

```csharp
public class CounterTreeNode : INotifyPropertyChanged
{
    // 既存のプロパティ...
    public bool IsWildCard { get; set; } = false; // 新規追加
    
    // 表示色の更新
    public string TextColor => Type switch
    {
        NodeType.Object => "DarkBlue",
        NodeType.Instance when IsWildCard => "Purple", // ワイルドカードは紫色
        NodeType.Instance => "DarkGreen", 
        NodeType.Counter when IsWildCard => "Purple", // ワイルドカードは紫色
        _ => "Black"
    };
}
```

### 2. BuildCounterTree メソッドの修正

複数のインスタンスを持つオブジェクトに自動的に "*" ノードを先頭に追加：

```csharp
// 複数のインスタンスがある場合、最初に "*" ノードを追加
bool hasMultipleInstances = objectGroup.Value.Count > 1 || 
                          (objectGroup.Value.Count == 1 && !objectGroup.Value.Keys.First().Equals("(なし)"));

if (hasMultipleInstances)
{
    // すべてのカウンター名を収集（重複を除去）
    var allCounterNames = new HashSet<string>();
    foreach (var instanceGroup in objectGroup.Value)
    {
        // カウンター名を抽出してセットに追加
    }
    
    // "*" インスタンスノードを作成
    var wildcardInstanceNode = new CounterTreeNode
    {
        DisplayName = "*",
        FullPath = "",
        Type = NodeType.Instance,
        IsWildCard = true
    };
    
    // "*" ノード下に各カウンターを追加
    foreach (var counterName in allCounterNames.OrderBy(x => x))
    {
        var wildcardCounterNode = new CounterTreeNode
        {
            DisplayName = counterName,
            FullPath = $"WILDCARD:{objectGroup.Key}:*:{counterName}",
            Type = NodeType.Counter,
            Parent = wildcardInstanceNode,
            IsWildCard = true
        };
        
        wildcardInstanceNode.Children.Add(wildcardCounterNode);
    }
    
    // オブジェクトノードの最初に追加
    objectNode.Children.Add(wildcardInstanceNode);
}
```

### 3. ワイルドカードパス形式

ワイルドカードカウンターのFullPathは以下の形式で管理：
```
WILDCARD:ObjectName:*:CounterName
```

例：`WILDCARD:Process:*:% Processor Time`

### 4. チェックボックス選択時の処理

ワイルドカードカウンターが選択された時に、対応するすべてのインスタンスのカウンターを自動選択：

```csharp
// ワイルドカードカウンターの場合、対応するすべてのインスタンスのカウンターを選択
if (node.IsWildCard && node.FullPath.StartsWith("WILDCARD:"))
{
    HandleWildcardCounterSelection(node, true);
}
```

### 5. HandleWildcardCounterSelection メソッド

```csharp
private void HandleWildcardCounterSelection(CounterTreeNode wildcardNode, bool isChecked)
{
    // WILDCARD:ObjectName:*:CounterName 形式からパースする
    var parts = wildcardNode.FullPath.Split(':');
    var objectName = parts[1];
    var counterName = parts[3];
    
    // 対応するオブジェクトノードを検索
    var objectNode = _counterTreeNodes.FirstOrDefault(o => o.DisplayName == objectName);
    
    // すべてのインスタンス（"*"以外）のカウンターを選択/選択解除
    foreach (var instanceNode in objectNode.Children)
    {
        if (instanceNode.IsWildCard) continue; // "*" ノード自体は除外
        
        var targetCounterNode = instanceNode.Children.FirstOrDefault(c => c.DisplayName == counterName);
        if (targetCounterNode != null)
        {
            targetCounterNode.IsChecked = isChecked;
        }
    }
}
```

### 6. GetSelectedCounters メソッドの修正

ワイルドカードカウンターを除外し、実際のカウンターのみを返すように修正：

```csharp
public IEnumerable<CounterTreeNode> GetSelectedCounters()
{
    if (IsLeaf && IsChecked == true && !IsWildCard)
    {
        yield return this;
    }
    
    foreach (var child in Children)
    {
        foreach (var selectedCounter in child.GetSelectedCounters())
        {
            yield return selectedCounter;
        }
    }
}
```

## UI表示例

実装後の階層構造例：

```
Process
├── * (ワイルドカード - 紫色表示)
│   ├── % Processor Time
│   └── Working Set
├── chrome
│   ├── % Processor Time
│   └── Working Set
└── notepad
    ├── % Processor Time
    └── Working Set

Memory (単一インスタンスなので * なし)
└── (総合)
    ├── Available MBytes
    └── Committed Bytes
```

## 技術的考慮事項

1. **無限ループ防止**: ワイルドカードカウンター選択時に他のカウンターを自動選択する際、イベントハンドラーの無限ループを防ぐ
2. **パフォーマンス**: 大量のインスタンスがある場合の処理速度
3. **UI応答性**: チェックボックス状態の更新時のUI応答性
4. **データ整合性**: ワイルドカード選択と個別選択の状態整合性

## テスト項目

1. 複数インスタンスオブジェクトに "*" ノードが自動生成される
2. 単一インスタンスオブジェクトに "*" ノードが生成されない
3. ワイルドカードカウンター選択時に対応する全インスタンスが選択される
4. ワイルドカードカウンター選択解除時に対応する全インスタンスが選択解除される
5. GetSelectedCounters でワイルドカードカウンターが除外される
6. UI上でワイルドカードノードが紫色で表示される

## フェーズ2: 凡例表示改善

### 課題
大量のインスタンス（プロセス等）を一括選択した場合、ScottPlotの標準凡例がグラフ領域を覆ってしまい、グラフが見えなくなる問題。

### 解決策
グラフエリアから凡例を分離し、独立したスクロール可能な凡例パネルを実装。

### 実装方法

#### 1. UI構造の変更

**MainWindow.xaml の修正:**
```xml
<!-- グラフと凡例の表示エリア -->
<Grid Grid.Row="1">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="5" x:Name="LegendSplitterColumn"/>
        <ColumnDefinition Width="250" MinWidth="150" MaxWidth="400" x:Name="LegendColumn"/>
    </Grid.ColumnDefinitions>
    
    <!-- ScottPlot グラフコンテナ（左側） -->
    <scottplot:WpfPlot x:Name="PerformanceChart" Grid.Column="0"/>
    
    <!-- グリッドスプリッター -->
    <GridSplitter Grid.Column="1" x:Name="LegendGridSplitter"/>
    
    <!-- 凡例表示エリア（右側） -->
    <GroupBox Grid.Column="2" x:Name="LegendGroupBox" Header="📋 凡例">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="LegendItemsControl"/>
        </ScrollViewer>
    </GroupBox>
</Grid>
```

#### 2. LegendItem データクラスの実装

```csharp
public class LegendItem : INotifyPropertyChanged
{
    private bool _isVisible = true;
    private string _currentValue = "";
    private Color _color = Colors.Blue;
    
    public string CounterName { get; set; } = string.Empty;
    public string CounterPath { get; set; } = string.Empty;
    
    public bool IsVisible
    {
        get => _isVisible;
        set
        {
            if (_isVisible != value)
            {
                _isVisible = value;
                OnPropertyChanged();
                OnPropertyChanged(nameof(BackgroundBrush));
                OnPropertyChanged(nameof(TextBrush));
            }
        }
    }
    
    public string CurrentValue { get; set; } // 最新値表示
    public Color Color { get; set; } // シリーズ色
    
    // UI用のBrushプロパティ
    public Brush ColorBrush => new SolidColorBrush(_color);
    public Brush BackgroundBrush => _isVisible ? Brushes.Transparent : new SolidColorBrush(Color.FromArgb(50, 200, 200, 200));
    public Brush TextBrush => _isVisible ? Brushes.Black : Brushes.Gray;
}
```

#### 3. 凡例管理機能

**主要メソッド:**
- `InitializeLegend()`: 凡例の初期化
- `ToggleLegend_Click()`: 凡例の表示/非表示切り替え
- `AddLegendItem()`: シリーズ追加時の凡例アイテム生成
- `RemoveLegendItem()`: シリーズ削除時の凡例アイテム削除
- `UpdateChartSeriesVisibility()`: グラフシリーズの表示/非表示制御
- `UpdateLegendCurrentValues()`: 最新値の更新

#### 4. ScottPlot凡例の無効化

```csharp
private void InitializeChart()
{
    // 凡例を無効化（独立した凡例コンポーネントを使用）
    PerformanceChart.Plot.Legend.IsVisible = false;
}
```

#### 5. 色の統一管理

```csharp
// ScottPlot.ColorをSystem.Windows.Media.Colorに変換
private System.Windows.Media.Color ConvertToMediaColor(ScottPlot.Color scottPlotColor)
{
    return System.Windows.Media.Color.FromArgb(
        scottPlotColor.A,
        scottPlotColor.R,
        scottPlotColor.G,
        scottPlotColor.B
    );
}
```

### UI機能

#### 凡例操作機能
1. **📋 凡例表示ボタン**: 凡例パネルの表示/非表示切り替え
2. **個別チェックボックス**: 各シリーズの表示/非表示制御
3. **「すべて表示」ボタン**: 全シリーズを一括表示
4. **「すべて非表示」ボタン**: 全シリーズを一括非表示

#### 表示情報
- **色見本**: シリーズの色を16x3pxの矩形で表示
- **カウンター名**: フルパス名をツールチップで表示
- **現在値**: 最新データポイントの値（K/M単位で短縮表示）

### 技術的改善点

1. **パフォーマンス向上**
   - ScottPlotの凡例描画処理を無効化
   - 大量シリーズでもスクロール可能

2. **視認性向上** 
   - グラフエリアを凡例が覆わない
   - 色見本でシリーズの識別が容易
   - 非表示シリーズはグレーアウト表示

3. **操作性向上**
   - ドラッグ可能なスプリッターで凡例幅調整
   - チェックボックスによる直感的な表示制御
   - 現在値のリアルタイム表示

## 実装完了事項 (更新)

### フェーズ1: ワイルドカード機能
- [x] CounterTreeNode クラスの拡張
- [x] BuildCounterTree メソッドの修正
- [x] チェックボックスイベントハンドラーの修正
- [x] ワイルドカード選択処理の実装
- [x] GetSelectedCounters メソッドの修正
- [x] UI表示スタイルの調整
- [x] 基本的なユニットテストの作成

### フェーズ2: 凡例表示改善
- [x] グラフエリアの2列分割UI実装
- [x] LegendItemクラスの実装
- [x] 独立した凡例リストコンポーネントの実装
- [x] 凡例の表示/非表示切り替え機能
- [x] 個別シリーズの表示/非表示制御
- [x] グラフシリーズ追加/削除時の凡例連動
- [x] ScottPlot凡例の無効化
- [x] 色の統一管理とMediaColor変換
- [x] 現在値表示機能
- [x] スクロール可能な凡例パネル

## 今後の改善点

1. より詳細なユニットテストの追加
2. 大量データでのパフォーマンステスト
3. ユーザビリティテスト
4. エラーハンドリングの強化
5. ワイルドカード機能のドキュメント更新