# 38-パフォーマンスモニターの取得間隔の表示

## タスク概要
PDHを使用してBLGファイルのカウンターのデータ取得間隔を取得し、ファイル名、コンピュータ名を表示している欄に取得間隔を追加する機能を実装しました。

## 問題と修正

### 発生した問題（2024年12月）
**症状**: 取得間隔の表示がない
**原因**: サンプル数が少ない場合（1個以下）に間隔が0になり、表示条件 `_samplingInterval != TimeSpan.Zero` を満たさないため非表示になっていた

### 修正内容
1. **サンプル数チェックの改善**: 単一サンプルの場合でも最小間隔（1秒）を返すようにした
2. **表示条件の変更**: 取得間隔が0の場合でも「取得間隔: 取得失敗」と表示
3. **デバッグ情報の強化**: サンプル数、開始・終了時刻の詳細ログ追加

## 実装した機能

### 1. データ取得間隔の計算と表示
- BLGファイルから時間範囲とサンプル数を取得
- 時間範囲 ÷ (サンプル数-1) で取得間隔を計算
- ユーザーフレンドリーな形式で表示（秒、分、時間単位）

### 2. UI要素の追加
- ファイル名、コンピュータ名の下に「取得間隔」を表示
- データが取得できない場合でも「取得失敗」と表示

## 技術的な実装詳細

### BlgFileAnalyzer.cs の変更（修正版）
```csharp
// 修正されたメソッド
public async Task<TimeSpan> GetSamplingIntervalAsync(IProgress<string>? progress = null)
{
    // ... 既存の処理 ...
    
    // 改善されたサンプル数チェック
    if (numEntries > 1)
    {
        var totalDuration = endTime - startTime;
        interval = TimeSpan.FromTicks(totalDuration.Ticks / (numEntries - 1));
        progress?.Report($"サンプリング間隔計算成功: {FormatInterval(interval)}");
    }
    else if (numEntries == 1)
    {
        progress?.Report($"警告: サンプル数が1のため間隔計算不可 (単一サンプル)");
        // 単一サンプルの場合でも最小間隔を返す
        interval = TimeSpan.FromSeconds(1);
    }
    else
    {
        progress?.Report($"警告: サンプル数が0のため間隔計算不可");
        throw new InvalidOperationException($"BLGファイルにサンプルデータが含まれていません");
    }
}
```

### MainWindow.xaml の変更
```xml
<!-- データ取得間隔表示用のTextBlockを追加 -->
<TextBlock x:Name="SamplingIntervalDisplay" 
           Text="" 
           Margin="5,45,5,10" 
           FontSize="11"
           Foreground="DarkGray"
           TextWrapping="Wrap"
           Visibility="Collapsed"/>
```

### MainWindow.xaml.cs の変更（修正版）
```csharp
// プライベート変数追加
private TimeSpan _samplingInterval = TimeSpan.Zero;

// 修正された表示条件
if (_samplingInterval != TimeSpan.Zero)
{
    SamplingIntervalDisplay.Text = $"取得間隔: {FormatSamplingInterval(_samplingInterval)}";
    SamplingIntervalDisplay.Visibility = Visibility.Visible;
}
else
{
    // 取得間隔が0の場合でも情報を表示
    SamplingIntervalDisplay.Text = "取得間隔: 取得失敗";
    SamplingIntervalDisplay.Visibility = Visibility.Visible;
}

// 改善されたエラーハンドリング
try
{
    _samplingInterval = await analyzer.GetSamplingIntervalAsync(progress);
    LogError($"Sampling interval extracted from BLG file: {_samplingInterval}");
    if (_samplingInterval == TimeSpan.Zero)
    {
        LogError("警告: 取得間隔が0です。単一サンプルまたはデータが不足している可能性があります。");
    }
}
catch (Exception ex)
{
    LogError($"Failed to extract sampling interval from BLG file: {ex.Message}");
    _samplingInterval = TimeSpan.Zero;
}
```

// ファイル読み込み時に取得間隔を取得・表示
_samplingInterval = await analyzer.GetSamplingIntervalAsync(progress);

// UI表示処理
if (_samplingInterval != TimeSpan.Zero)
{
    SamplingIntervalDisplay.Text = $"取得間隔: {FormatSamplingInterval(_samplingInterval)}";
    SamplingIntervalDisplay.Visibility = Visibility.Visible;
}

// フォーマット関数
private string FormatSamplingInterval(TimeSpan interval)
{
    // 時間、分、秒、ミリ秒単位での適切な表示
}
```

## PDH API の活用
- `PdhGetDataSourceTimeRange` APIを使用してBLGファイルのメタデータを取得
- 開始時間、終了時間、サンプル数から取得間隔を算出
- エラーハンドリングにより、データが取得できない場合は適切に処理

## 表示仕様
- **取得間隔の形式**: 
  - 1時間以上: "X.X時間"
  - 1分以上: "X.X分"
  - 1秒以上: "X.X秒"
  - 1ミリ秒以上: "Xミリ秒"
  - データなし: 非表示

## 動作テスト結果
- フォーマット関数のテストが正常に動作することを確認
- 各時間単位での表示が適切であることを検証

## 今後の拡張可能性
1. 取得間隔の詳細情報（最小/最大間隔）の表示
2. 取得間隔に基づく推奨設定の提案
3. 複数BLGファイルの比較時の取得間隔比較機能

## 注意事項
- Windows環境のPDH APIに依存するため、Windows専用機能
- BLGファイルの形式や破損により取得間隔が取得できない場合があります
- 取得間隔が0の場合は非表示として扱われます

## トラブルシューティング
- 取得間隔が表示されない場合: BLGファイルの形式確認、サンプル数の確認
- エラーが発生する場合: PDH APIの権限、BLGファイルの破損チェック