# 36-グラフタイプ切り替え機能の実装

## 概要

Issue #36として、折れ線グラフと積み重ね面グラフの切り替え機能を実装した。デフォルトは折れ線グラフで、ユーザーがグラフタイプをラジオボタンで選択できるようにした。

## 要件

- 折れ線グラフと積み重ね面グラフに相互に切り替えられる機能
- デフォルトは折れ線グラフ

## 実装したファイルと変更内容

### 1. MainWindow.xaml の変更

**変更点:**
- グラフ表示エリアの上部にグラフタイプ選択のUIを追加
- 3行のRowDefinitionに変更（グラフタイプ選択、グラフ、統計情報）

**追加したUI:**
```xml
<!-- グラフタイプ選択 -->
<Border Grid.Row="0" Background="#F8F8F8" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Margin="5,5,5,0">
    <StackPanel Orientation="Horizontal" Margin="10,8">
        <TextBlock Text="📊 グラフタイプ:" 
                 VerticalAlignment="Center" 
                 FontWeight="SemiBold" 
                 Margin="0,0,15,0"/>
        <RadioButton x:Name="LineChartRadio" 
                   Content="折れ線グラフ" 
                   IsChecked="True" 
                   GroupName="ChartType"
                   Checked="ChartType_Changed"
                   Margin="0,0,20,0"
                   VerticalAlignment="Center"/>
        <RadioButton x:Name="StackedAreaChartRadio" 
                   Content="積み重ね面グラフ" 
                   GroupName="ChartType"
                   Checked="ChartType_Changed"
                   VerticalAlignment="Center"/>
    </StackPanel>
</Border>
```

### 2. MainWindow.xaml.cs の変更

#### 新しいenum定義
```csharp
/// <summary>
/// グラフタイプの列挙型
/// </summary>
public enum ChartType
{
    LineChart,        // 折れ線グラフ
    StackedAreaChart  // 積み重ね面グラフ
}
```

#### 新しいフィールド
```csharp
private readonly Dictionary<string, ScottPlot.Plottables.FillY> _areaChartSeries = new();
private ChartType _currentChartType = ChartType.LineChart;
```

#### 主要な新規メソッド

1. **ChartType_Changed** - グラフタイプ変更イベントハンドラー
2. **RefreshChartWithCurrentType** - 現在のタイプでチャート全体を再描画
3. **DrawLineChart** - 折れ線グラフ描画
4. **DrawStackedAreaChart** - 積み重ね面グラフ描画
5. **InterpolateValue** - データポイント間の線形補間
6. **GetNextColor** - 積み重ね面グラフ用の色管理
7. **AddLineChartSeries** - 個別の折れ線シリーズ追加

#### 修正したメソッド

1. **AddCounterToChart** - 現在のチャートタイプに応じた処理分岐
2. **RemoveCounterFromChart** - 両方のチャートタイプのシリーズ削除対応
3. **UpdateChartVisibility** - 両方のチャートタイプのデータ有無確認
4. **UpdateStatisticsDisplay** - 両方のチャートタイプのカウンター統計
5. **RefreshCounterInChart** - スケール変更時の再描画対応
6. **UpdateScaleControlVisibility** - 現在のチャートタイプのカウンター表示
7. **InitializeChartWithSelectedCounters** - チャートタイプ別の初期化

## 技術的な実装詳細

### 折れ線グラフ（LineChart）
- `ScottPlot.Plot.Add.Scatter()` を使用
- 個別のシリーズとして管理
- カウンターの追加・削除が個別に可能

### 積み重ね面グラフ（StackedAreaChart）
- `ScottPlot.Plot.Add.FillY()` を使用
- ベースライン累積方式で実装
- 全体再描画が必要（一つのカウンターの変更で全て再計算）

### データ補間機能
積み重ね面グラフでは、各カウンターのタイムスタンプが一致しない場合があるため、線形補間を実装：

```csharp
private double InterpolateValue(List<PerformanceDataPoint> dataPoints, DateTime targetTime, double scale)
{
    // 目標時間の前後のデータポイントを見つけて線形補間
    var before = dataPoints.Where(dp => dp.Timestamp <= targetTime).LastOrDefault();
    var after = dataPoints.Where(dp => dp.Timestamp >= targetTime).FirstOrDefault();
    
    // 線形補間の計算...
}
```

### 色管理
積み重ね面グラフ用に10色のカラーパレットを定義：

```csharp
private ScottPlot.Color GetNextColor(int index)
{
    var colors = new[]
    {
        ScottPlot.Colors.Blue, ScottPlot.Colors.Red, ScottPlot.Colors.Green,
        ScottPlot.Colors.Orange, ScottPlot.Colors.Purple, ScottPlot.Colors.Brown,
        ScottPlot.Colors.Pink, ScottPlot.Colors.Gray, ScottPlot.Colors.Olive,
        ScottPlot.Colors.Cyan
    };
    
    return colors[index % colors.Length];
}
```

## 動作フロー

### グラフタイプ変更時
1. ラジオボタンの選択変更
2. `ChartType_Changed` イベントハンドラー実行
3. `_currentChartType` を更新
4. `RefreshChartWithCurrentType()` で全体再描画

### カウンター追加時
1. **折れ線グラフ**: `AddLineChartSeries()` で個別追加
2. **積み重ね面グラフ**: `RefreshChartWithCurrentType()` で全体再描画

### スケール変更時
1. **折れ線グラフ**: 該当シリーズのみ再描画
2. **積み重ね面グラフ**: 全体再描画

## テスト方法

1. BLGファイルを読み込み
2. 複数のカウンターを選択
3. ラジオボタンでグラフタイプを切り替え
4. 追加カウンターの選択/選択解除
5. スケール変更機能のテスト

## 注意点

### パフォーマンス
- 積み重ね面グラフは全体再描画が必要なため、大量のカウンターでは重い
- データ補間処理によりCPU使用量が増加する可能性

### データ補間の精度
- 線形補間を使用しているため、実際のデータポイント間の変化を正確に表現しない場合がある
- 時間間隔が大きい場合は補間の精度が落ちる

### 色の制限
- 10色までの定義済みパレットを使用
- 10色を超えるカウンターの場合は色が重複する

## 今後の改善案

1. **パフォーマンス最適化**
   - 積み重ね面グラフの部分更新機能
   - データ補間のキャッシュ機能

2. **補間方法の改善**
   - スプライン補間の追加オプション
   - ステップ補間の実装

3. **UI/UX改善**
   - グラフタイプのプレビュー機能
   - カラーパレットの拡張または設定可能化

4. **設定の永続化**
   - 選択したグラフタイプの設定保存
   - ユーザー定義のカラーパレット保存

## まとめ

折れ線グラフと積み重ね面グラフの切り替え機能を正常に実装できた。ScottPlot.WPFの機能を活用し、最小限のコード変更で要件を満たすことができた。積み重ね面グラフの実装では、データ補間や色管理など追加の技術的課題も解決した。