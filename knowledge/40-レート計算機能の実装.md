# レート計算機能の実装

## 概要
Performance Monitor AnalyzerにWindows Performance Monitor の .blg ファイルのグラフ表示において、生値表示とレート計算値表示を切り替えることができる機能を実装しました。

## 要件
- グラフ表示時に、値をそのまま表示する場合と、連続した値間の差を出して、パフォーマンスモニターの取得間隔で割ったレート算出した値の二種類の方法でグラフを作成できるように切り替えられる機能

## 実装内容

### 1. UI変更 (MainWindow.xaml)
- グラフ表示エリアの上部にレート計算モード切り替えコントロールを追加
- 2つのラジオボタンで「生値表示」と「レート計算値表示」を切り替え可能
- 視覚的に分かりやすいボーダーと背景色を設定

#### 変更箇所
```xml
<!-- グラフ表示モード選択 -->
<Border Grid.Row="0" Background="#F0F8FF" BorderBrush="#C0C0C0" BorderThickness="0,0,0,1" Margin="5,5,5,0" Padding="8,5">
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
        <TextBlock Text="📊 表示モード:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>
        <RadioButton x:Name="RawValueRadioButton" 
                   Content="生値表示" 
                   IsChecked="True" 
                   VerticalAlignment="Center" 
                   Margin="0,0,15,0"
                   Checked="DisplayMode_Changed"
                   ToolTip="パフォーマンスカウンターの値をそのまま表示"/>
        <RadioButton x:Name="RateCalculationRadioButton" 
                   Content="レート計算値表示" 
                   VerticalAlignment="Center"
                   Checked="DisplayMode_Changed"
                   ToolTip="連続した値の差分を取得間隔で割ったレート値を表示"/>
    </StackPanel>
</Border>
```

### 2. バックエンド実装 (MainWindow.xaml.cs)

#### レート計算フラグの追加
```csharp
// レート計算モードのフラグ
private bool _isRateCalculationMode = false;
```

#### レート計算アルゴリズムの実装
```csharp
/// <summary>
/// データポイントのリストからレート計算を行い、新しいデータポイントリストを返す
/// </summary>
/// <param name="dataPoints">元のデータポイント</param>
/// <returns>レート計算されたデータポイント</returns>
private List<PerformanceDataPoint> CalculateRateValues(List<PerformanceDataPoint> dataPoints)
{
    if (dataPoints.Count < 2)
    {
        return new List<PerformanceDataPoint>();
    }

    var rateDataPoints = new List<PerformanceDataPoint>();
    
    for (int i = 1; i < dataPoints.Count; i++)
    {
        var currentPoint = dataPoints[i];
        var previousPoint = dataPoints[i - 1];
        
        // 時間間隔を計算（秒単位）
        var timeSpan = currentPoint.Timestamp - previousPoint.Timestamp;
        var intervalSeconds = timeSpan.TotalSeconds;
        
        // 時間間隔が0の場合はスキップ
        if (intervalSeconds <= 0)
        {
            continue;
        }
        
        // 値の差分を計算
        var valueDifference = currentPoint.Value - previousPoint.Value;
        
        // レート（差分/時間間隔）を計算
        var rateValue = valueDifference / intervalSeconds;
        
        // レート計算されたデータポイントを作成
        var rateDataPoint = new PerformanceDataPoint
        {
            Timestamp = currentPoint.Timestamp,
            Value = rateValue,
            FormattedValue = FormatValueWithUnit(rateValue, "/sec")
        };
        
        rateDataPoints.Add(rateDataPoint);
    }
    
    return rateDataPoints;
}
```

#### モード切り替えイベントハンドラー
```csharp
/// <summary>
/// 表示モード変更時のイベントハンドラー
/// </summary>
private void DisplayMode_Changed(object sender, RoutedEventArgs e)
{
    try
    {
        if (sender is RadioButton radioButton && radioButton.IsChecked == true)
        {
            // レート計算モードの状態を更新
            _isRateCalculationMode = radioButton.Name == "RateCalculationRadioButton";
            
            System.Diagnostics.Debug.WriteLine($"Display mode changed to: {(_isRateCalculationMode ? "Rate Calculation" : "Raw Values")}");
            
            // 既存のグラフを全て再描画
            RefreshAllChartsWithCurrentMode();
        }
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"Error in DisplayMode_Changed: {ex.Message}");
        LogError($"Error in DisplayMode_Changed: {ex}");
    }
}
```

#### 既存チャートの再描画機能
```csharp
/// <summary>
/// 現在のモードで全てのチャートを再描画
/// </summary>
private void RefreshAllChartsWithCurrentMode()
{
    try
    {
        // 現在チャートに表示されているカウンターのリストを取得
        var currentCounters = _chartSeries.Keys.ToList();
        
        // 一旦全てのシリーズをクリア
        PerformanceChart.Plot.Clear();
        _chartSeries.Clear();
        
        // 各カウンターを新しいモードで再描画
        foreach (var counter in currentCounters)
        {
            AddCounterToChartInternal(counter);
        }
        
        // グラフを更新
        PerformanceChart.Plot.Axes.AutoScale();
        PerformanceChart.Refresh();
        
        System.Diagnostics.Debug.WriteLine($"Refreshed {currentCounters.Count} chart series with {(_isRateCalculationMode ? "rate calculation" : "raw values")} mode");
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"Error in RefreshAllChartsWithCurrentMode: {ex.Message}");
        LogError($"Error in RefreshAllChartsWithCurrentMode: {ex}");
    }
}
```

#### 既存グラフ描画メソッドの修正
AddCounterToChart()とAddCounterToChartInternal()メソッドに以下の処理を追加：

```csharp
// レート計算モードが有効な場合はレート計算を適用
if (_isRateCalculationMode)
{
    dataPoints = CalculateRateValues(dataPoints);
    if (!dataPoints.Any())
    {
        System.Diagnostics.Debug.WriteLine($"No rate data points calculated for counter: {counter}");
        return;
    }
}
```

レジェンドにもレート表示の印を追加：
```csharp
var displayName = GetCounterDisplayName(counter);
scatter.LegendText = _isRateCalculationMode ? $"{displayName} (レート)" : displayName;
```

## レート計算アルゴリズムの詳細

1. **時間間隔の計算**: 連続する2つのデータポイント間のタイムスタンプ差分を秒単位で計算
2. **値の差分計算**: 現在の値から前の値を引いて差分を算出
3. **レート計算**: 差分を時間間隔で割ってレート（変化率/秒）を計算
4. **エッジケース処理**: 
   - データポイントが2つ未満の場合は空のリストを返す
   - 時間間隔が0以下の場合はそのポイントをスキップ

## 実装した機能

### ✅ 完了した機能
- UI上でのモード切り替え（ラジオボタン）
- レート計算アルゴリズムの実装
- モード切り替え時の全グラフ再描画
- グラフレジェンドでのレート表示の区別
- エラーハンドリングとデバッグログ

### 💡 技術的な考慮事項
- パフォーマンスカウンターの種類（累積型/瞬間型）に関わらず、差分計算でレートを算出
- 時間間隔は秒単位で計算し、結果は"xxx/sec"形式で表示
- 既存のスケール機能との互換性を保持
- メモリ効率を考慮し、レート計算は必要時のみ実行

## 使用方法

1. BLGファイルを開く
2. 任意のパフォーマンスカウンターを選択してグラフ表示
3. グラフエリア上部の「表示モード」で「レート計算値表示」を選択
4. グラフが自動的にレート計算値で再描画される
5. 「生値表示」に戻すことで元の値での表示に切り替わる

## テスト推奨項目

- [ ] サンプルBLGファイルでの動作確認
- [ ] 生値とレート値のモード切り替え動作確認
- [ ] 複数カウンター同時表示時の切り替え動作
- [ ] レジェンド表示の確認
- [ ] エラーケース（データ不足、時間間隔異常等）の処理確認

## 関連ファイル

- `src/PerformanceMonitorAnalyzer/MainWindow.xaml` - UI定義
- `src/PerformanceMonitorAnalyzer/MainWindow.xaml.cs` - ロジック実装