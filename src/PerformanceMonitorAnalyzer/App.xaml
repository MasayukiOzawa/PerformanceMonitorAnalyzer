<Application x:Class="PerformanceMonitorAnalyzer.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <!-- Modern UI Color Palette -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#6750A4"/>
        <SolidColorBrush x:Key="PrimaryVariantColor" Color="#4A2C86"/>
        <SolidColorBrush x:Key="SecondaryColor" Color="#625B71"/>
        <SolidColorBrush x:Key="SurfaceColor" Color="#FFFBFE"/>
        <SolidColorBrush x:Key="BackgroundColor" Color="#FFFBFE"/>
        <SolidColorBrush x:Key="ErrorColor" Color="#BA1A1A"/>
        <SolidColorBrush x:Key="OnPrimaryColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="OnSecondaryColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="OnSurfaceColor" Color="#1C1B1F"/>
        <SolidColorBrush x:Key="OnBackgroundColor" Color="#1C1B1F"/>
        <SolidColorBrush x:Key="OnErrorColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="OutlineColor" Color="#79747E"/>
        <SolidColorBrush x:Key="SurfaceVariantColor" Color="#E7E0EC"/>
        <SolidColorBrush x:Key="OnSurfaceVariantColor" Color="#49454F"/>
        <SolidColorBrush x:Key="SuccessColor" Color="#2E7D32"/>
        <SolidColorBrush x:Key="WarningColor" Color="#F57C00"/>
        <SolidColorBrush x:Key="InfoColor" Color="#1976D2"/>
        
        <!-- Dark Theme Colors -->
        <SolidColorBrush x:Key="DarkPrimaryColor" Color="#D0BCFF"/>
        <SolidColorBrush x:Key="DarkPrimaryVariantColor" Color="#B8A9FF"/>
        <SolidColorBrush x:Key="DarkSecondaryColor" Color="#CCC2DC"/>
        <SolidColorBrush x:Key="DarkSurfaceColor" Color="#1C1B1F"/>
        <SolidColorBrush x:Key="DarkBackgroundColor" Color="#121212"/>
        <SolidColorBrush x:Key="DarkErrorColor" Color="#F2B8B5"/>
        <SolidColorBrush x:Key="DarkOnPrimaryColor" Color="#381E72"/>
        <SolidColorBrush x:Key="DarkOnSecondaryColor" Color="#332D41"/>
        <SolidColorBrush x:Key="DarkOnSurfaceColor" Color="#E6E1E5"/>
        <SolidColorBrush x:Key="DarkOnBackgroundColor" Color="#E6E1E5"/>
        <SolidColorBrush x:Key="DarkOnErrorColor" Color="#601410"/>
        <SolidColorBrush x:Key="DarkOutlineColor" Color="#938F99"/>
        <SolidColorBrush x:Key="DarkSurfaceVariantColor" Color="#49454F"/>
        <SolidColorBrush x:Key="DarkOnSurfaceVariantColor" Color="#CAC4D0"/>
        
        <!-- Animation Resources -->
        <Duration x:Key="FastAnimation">0:0:0.2</Duration>
        <Duration x:Key="MediumAnimation">0:0:0.3</Duration>
        <Duration x:Key="SlowAnimation">0:0:0.5</Duration>
        
        <!-- Modern Card Style -->
        <Style x:Key="ModernCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource OutlineColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" Direction="270" ShadowDepth="2" Color="#40000000"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Modern GroupBox Style -->
        <Style x:Key="ModernGroupBox" TargetType="GroupBox">
            <Setter Property="Background" Value="{StaticResource SurfaceColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource OutlineColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Foreground" Value="{StaticResource OnSurfaceColor}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12"
                                Padding="0">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="8" Direction="270" ShadowDepth="2" Color="#20000000"/>
                            </Border.Effect>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Border Grid.Row="0" Background="{StaticResource PrimaryColor}" 
                                        CornerRadius="12,12,0,0" Padding="16,12">
                                    <ContentPresenter ContentSource="Header" 
                                                    TextElement.Foreground="{StaticResource OnPrimaryColor}"
                                                    TextElement.FontWeight="SemiBold"
                                                    TextElement.FontSize="14"/>
                                </Border>
                                <ContentPresenter Grid.Row="1" Margin="{TemplateBinding Padding}"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Modern Button Style with Animations -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="{StaticResource OnPrimaryColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="20"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect x:Name="ButtonShadow" BlurRadius="4" Direction="270" ShadowDepth="1" Color="#40000000"/>
                            </Border.Effect>
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="ButtonScale" ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="ButtonBorder"
                                                          Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                          To="#4A2C86" Duration="{StaticResource FastAnimation}"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonShadow"
                                                           Storyboard.TargetProperty="BlurRadius"
                                                           To="8" Duration="{StaticResource FastAnimation}"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonShadow"
                                                           Storyboard.TargetProperty="ShadowDepth"
                                                           To="2" Duration="{StaticResource FastAnimation}"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonScale"
                                                           Storyboard.TargetProperty="ScaleX"
                                                           To="1.05" Duration="{StaticResource FastAnimation}"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonScale"
                                                           Storyboard.TargetProperty="ScaleY"
                                                           To="1.05" Duration="{StaticResource FastAnimation}"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="ButtonBorder"
                                                          Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                          To="#6750A4" Duration="{StaticResource FastAnimation}"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonShadow"
                                                           Storyboard.TargetProperty="BlurRadius"
                                                           To="4" Duration="{StaticResource FastAnimation}"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonShadow"
                                                           Storyboard.TargetProperty="ShadowDepth"
                                                           To="1" Duration="{StaticResource FastAnimation}"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonScale"
                                                           Storyboard.TargetProperty="ScaleX"
                                                           To="1" Duration="{StaticResource FastAnimation}"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonScale"
                                                           Storyboard.TargetProperty="ScaleY"
                                                           To="1" Duration="{StaticResource FastAnimation}"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonScale" Property="ScaleX" Value="0.95"/>
                                <Setter TargetName="ButtonScale" Property="ScaleY" Value="0.95"/>
                                <Setter TargetName="ButtonShadow" Property="BlurRadius" Value="2"/>
                                <Setter TargetName="ButtonShadow" Property="ShadowDepth" Value="0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource OutlineColor}"/>
                                <Setter Property="Foreground" Value="{StaticResource OnSurfaceVariantColor}"/>
                                <Setter TargetName="ButtonShadow" Property="BlurRadius" Value="0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Modern Secondary Button Style -->
        <Style x:Key="ModernSecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="20"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource SurfaceVariantColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource OutlineColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource SurfaceVariantColor}"/>
            <Setter Property="Foreground" Value="{StaticResource OnSurfaceVariantColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource OutlineColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Modern ComboBox Style -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource SurfaceVariantColor}"/>
            <Setter Property="Foreground" Value="{StaticResource OnSurfaceVariantColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource OutlineColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <Grid>
                                <ToggleButton x:Name="ToggleButton" 
                                            Grid.Column="2" 
                                            Focusable="false"
                                            IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                            ClickMode="Press"
                                            Background="Transparent"
                                            BorderThickness="0">
                                    <Path x:Name="Arrow" Fill="{StaticResource OnSurfaceVariantColor}" 
                                          HorizontalAlignment="Right" VerticalAlignment="Center" 
                                          Margin="0,0,12,0"
                                          Data="M 0 0 L 4 4 L 8 0 Z"/>
                                </ToggleButton>
                                <ContentPresenter Name="ContentSite" IsHitTestVisible="False" 
                                                Content="{TemplateBinding SelectionBoxItem}"
                                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                Margin="{TemplateBinding Padding}"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Left"/>
                                <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" 
                                       AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                    <Border Background="{StaticResource SurfaceColor}" 
                                            BorderBrush="{StaticResource OutlineColor}"
                                            BorderThickness="1" CornerRadius="8" 
                                            MinWidth="{TemplateBinding ActualWidth}">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="8" Direction="270" ShadowDepth="2" Color="#40000000"/>
                                        </Border.Effect>
                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Modern RadioButton Style -->
        <Style x:Key="ModernRadioButton" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource OnSurfaceColor}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="RadioBorder" Width="20" Height="20" 
                                    Background="{StaticResource SurfaceColor}"
                                    BorderBrush="{StaticResource OutlineColor}"
                                    BorderThickness="2" CornerRadius="10"
                                    VerticalAlignment="Center" Margin="0,0,8,0">
                                <Ellipse x:Name="RadioCheck" Width="10" Height="10" 
                                         Fill="{StaticResource PrimaryColor}" Opacity="0"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="RadioCheck" Property="Opacity" Value="1"/>
                                <Setter TargetName="RadioBorder" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RadioBorder" Property="Background" Value="{StaticResource SurfaceVariantColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Modern Progress Bar Style -->
        <Style x:Key="ModernProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="6"/>
            <Setter Property="Background" Value="{StaticResource SurfaceVariantColor}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3">
                            <Rectangle x:Name="PART_Track" Fill="{TemplateBinding Foreground}" 
                                     RadiusX="3" RadiusY="3"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>